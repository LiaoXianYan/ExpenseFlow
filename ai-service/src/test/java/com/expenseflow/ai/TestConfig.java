package com.expenseflow.ai;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestConfig {
    // Spring Boot test context configuration — beans mocked via @MockBean in BaseControllerTest
}
