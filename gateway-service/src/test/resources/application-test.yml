spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
    sentinel:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: false
  data:
    redis:
      host: localhost
      port: 6379
  main:
    web-application-type: reactive
